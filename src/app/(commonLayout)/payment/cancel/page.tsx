import PaymentCancelled from "@/components/modules/payment/PaymentCancelled";

const page = () => {
    return (
        <div>
            <PaymentCancelled/>
        </div>
    );
};

export default page;